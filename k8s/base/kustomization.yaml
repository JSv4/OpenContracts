apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Base Kubernetes resources for OpenContracts
resources:
  # Storage
  - postgres-data-pvc.yaml
  - postgres-backups-pvc.yaml

  # Core services
  - postgres-deployment.yaml
  - postgres-service.yaml
  - redis-deployment.yaml
  - redis-service.yaml

  # Django application
  - django-deployment.yaml
  - django-service.yaml

  # Celery workers
  - celeryworker-deployment.yaml
  - celerybeat-deployment.yaml

  # Microservices
  - docling-parser-deployment.yaml
  - docling-parser-service.yaml
  - vector-embedder-deployment.yaml
  - vector-embedder-service.yaml
