apiVersion: apps/v1
kind: Deployment
metadata:
  name: celeryworker
spec:
  template:
    spec:
      containers:
      - name: celeryworker
        env:
        - name: DJANGO_SETTINGS_MODULE
          value: "config.settings.test"
