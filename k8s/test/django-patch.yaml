apiVersion: apps/v1
kind: Deployment
metadata:
  name: django
spec:
  template:
    spec:
      containers:
      - name: django
        env:
        - name: DJANGO_SETTINGS_MODULE
          value: "config.settings.test"
        command: ["/bin/bash", "-c"]
        args: ["python manage.py migrate && /start"]
